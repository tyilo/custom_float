moddef::moddef!(
    flat(pub) mod {
        arithmetic,
        bytes,
        rational
    },
    flat(pub) mod {
        clamp,
        midpoint,
        next,
        polynomial,
        pow,
        round
    }
);