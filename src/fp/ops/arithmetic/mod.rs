moddef::moddef!(
    flat(pub) mod {
        add,
        div,
        divb,
        euclid,
        inv,
        mul_add,
        mul,
        mulb,
        neg,
        rem,
        squared,
        sub
    }
);